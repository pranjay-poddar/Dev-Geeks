/*
 * SignupUserPage Messages
 *
 * This contains all the text for the SignupUserPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.SignupUserPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SignupUserPage container!',
  },
});
