/*
 * SubModules Messages
 *
 * This contains all the text for the SubModules container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.SubModules';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SubModules container!',
  },
});
