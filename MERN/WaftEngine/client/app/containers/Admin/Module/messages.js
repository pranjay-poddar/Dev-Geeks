/*
 * Module Messages
 *
 * This contains all the text for the Module container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Module';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Module container!',
  },
});
