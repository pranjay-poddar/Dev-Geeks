/*
 * GlobalSetting Messages
 *
 * This contains all the text for the GlobalSetting container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.GlobalSetting';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the GlobalSetting container!',
  },
});
