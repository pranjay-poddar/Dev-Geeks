/*
 * AdminRole Messages
 *
 * This contains all the text for the AdminRole container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AdminRole';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AdminRole container!',
  },
});
